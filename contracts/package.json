{
  "name": "vault-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Vault Protocol",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:fuji": "hardhat run scripts/deploy.ts --network fuji"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.1"
  }
}
