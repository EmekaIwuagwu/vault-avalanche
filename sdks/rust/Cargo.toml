[package]
name = "vault-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for Vault Protocol"

[dependencies]
reqwest = { version = "0.11", features = ["json", "multipart", "blocking"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
